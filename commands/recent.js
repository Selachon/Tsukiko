import { SlashCommandBuilder as SCB } from 'discord.js'

export default {
  data: new SCB()
    .setName('')
    .setDescription(''),
  category: '',
  usage: `/ `,
  execute: async (Tsukiko, interaction) => {
    
  },
}